## This tool has no MCP client

The search tools are not tool calls here. They are a program you run through
`bash`. The interface is always `cli` — do not probe for MCP, and never report
the search layer as missing.

Read `defaults/search_via_cli.md` before the first search of a session. It holds
the commands, the `batch` form to prefer, and what each failure reason means.
